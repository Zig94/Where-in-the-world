import { ReactNode } from 'react'

const CountryDetailBox = ({ children }: { children: ReactNode }) => {
	return <div className="details-country-box">{children}</div>
}
export default CountryDetailBox
